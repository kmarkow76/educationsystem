-- Сначала таблицы без зависимостей
CREATE TABLE suppliers (
    id SERIAL NOT NULL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    phone VARCHAR(16) NOT NULL,
    address VARCHAR(100)
);

CREATE TABLE employees (
    id SERIAL NOT NULL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    phone VARCHAR(16) NOT NULL
);

CREATE TABLE customers (
    id SERIAL NOT NULL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    phone VARCHAR(16) NOT NULL,
    is_regular BOOLEAN
);

CREATE TABLE drugs (
    id SERIAL NOT NULL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    category VARCHAR(100) NOT NULL,
    manufacturer VARCHAR(100) NOT NULL,
    price DECIMAL(10,2) NOT NULL,
    quantity_in_stock INT NOT NULL,
    expiration_date DATE NOT NULL,
    availability_status BOOLEAN
);

-- Затем таблицы с внешними ключами
CREATE TABLE sales (
    id SERIAL NOT NULL PRIMARY KEY,
    drug_id INT NOT NULL REFERENCES drugs(id),
    customer_id INT NOT NULL REFERENCES customers(id),
    quantity INT NOT NULL,
    unit_price DECIMAL(10,2) NOT NULL,
    sale_date DATE NOT NULL
);

CREATE TABLE receipts (
    id SERIAL NOT NULL PRIMARY KEY,
    drug_id INT NOT NULL REFERENCES drugs(id),
    supplier_id INT NOT NULL REFERENCES suppliers(id),
    quantity INT NOT NULL,
    receipt_price DECIMAL(10,2) NOT NULL,
    receipt_date DATE NOT NULL
);

-- Поставщики
INSERT INTO suppliers (name, phone, address) VALUES
    ('ФармаПлюс',      '+79001112233', 'Москва, ул. Ленина, 12'),
    ('МедСнаб',        '+79002223344', 'Санкт-Петербург, пр. Мира, 5'),
    ('БиоФарм',        '+79003334455', 'Казань, ул. Баумана, 33');

-- Сотрудники
INSERT INTO employees (name, phone) VALUES
    ('Иванова Анна Сергеевна',   '+79111234567'),
    ('Петров Дмитрий Олегович',  '+79117654321'),
    ('Сидорова Елена Викторовна','+79119876543');

-- Покупатели (двое постоянных, один новый)
INSERT INTO customers (name, phone, is_regular) VALUES
    ('Козлов Андрей Иванович',   '+79221112233', TRUE),
    ('Миронова Светлана Петровна','+79222223344', TRUE),
    ('Захаров Олег Николаевич',  '+79223334455', FALSE);

-- Препараты
INSERT INTO drugs (name, category, manufacturer, price, quantity_in_stock, expiration_date, availability_status) VALUES
    ('Парацетамол 500мг', 'Анальгетики',     'Фармстандарт', 85.00,  120, '2026-12-01', TRUE),
    ('Амоксициллин 250мг','Антибиотики',     'Синтез',       320.00,  45, '2025-11-01', TRUE),
    ('Но-шпа 40мг',       'Спазмолитики',   'Хиноин',       195.50,  80, '2027-03-15', TRUE);

-- Поступления
INSERT INTO receipts (drug_id, supplier_id, quantity, receipt_price, receipt_date) VALUES
    (1, 1, 200, 60.00,  '2025-01-10'),
    (2, 2, 100, 250.00, '2025-02-05'),
    (3, 3, 150, 150.00, '2025-03-20');

-- Продажи
INSERT INTO sales (drug_id, customer_id, quantity, unit_price, sale_date) VALUES
    (1, 1, 3,  85.00,  '2025-04-01'),  -- сумма 255 руб, скидки нет,  постоянный +5% → 5%
    (2, 2, 6,  320.00, '2025-04-03'),  -- сумма 1920 руб, скидка 10%, постоянный +5% → 15%
    (3, 3, 4,  195.50, '2025-04-07');  -- сумма 782 руб, скидка 5%,  не постоянный → 5%